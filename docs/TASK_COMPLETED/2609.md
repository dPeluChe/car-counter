# Tareas completadas: septiembre 2026

## 2026-09-14: Detección compartida, eventos por ruta, replay y cámara (PR #4)

### TODO-028: Referencias humanas de detección y rutas
- [x] Extracción y deduplicación de imágenes con `scripts/extract_validation_frames.py`.
- [x] Preetiquetas sin sobrescribir anotaciones existentes, con `flags.reviewed=false`, en `scripts/pre_label_frames.py`.
- [x] Evaluación de cajas/clases con el perfil real en `scripts/evaluate_pipeline.py`; exige revisión y rechaza imágenes faltantes o referencias duplicadas.
- [x] Evaluador de eventos con referencia `reviewed=true`, hash y límites de frames (`scripts/validate_routes.py --events`).

### TODO-029: Movimiento de cámara y geometría del conteo
- [x] Estimación de deriva del fondo (`carcounter/camera_motion.py`) y detención opcional por CLI/JSON (`--camera-max-drift-px`).
- [x] Pruebas de traslación, rotación, coordenadas originales, falta de textura y rechazo de resultado incompleto (`tests/test_camera_motion.py`).

### TODO-030: Validación de eventos y continuidad de IDs
- [x] Exportación de cada evento con frame de confirmación, ID, clase, ruta y sentido u origen/destino, conservando totales y eventos de tracks purgados.
- [x] Emparejamiento uno a uno por tiempo, ruta y clase con precisión, recall, F1, omisiones y predicciones sin pareja.
- [x] Referencia revisada obligatoria, mismo SHA-256 y tramo válido; una referencia vacía no aprueba.
- [x] Consenso de clase por track hasta el primer conteo, conservado en el resultado (`carcounter/counting.py`).

### TODO-032: Calidad de detección de autos
- [x] Detector y filtros compartidos entre calibración y runtime; clases desde los pesos; SAHI entrega cajas globales a un único tracker.

### TODO-034: Cron local de revisión
- [x] `scripts/recurring_review.py`, prompt, guía y pruebas de condiciones y lanzamiento simulado (`tests/test_recurring_review.py`).

### TODO-035: Clases equivocadas con RF-DETR `added: 2026-09-14`
- [x] `carcounter/rfdetr_detector.py:rfdetr_class_names()` resuelve nombres según el checkpoint: ids crudos de `COCO_CLASSES` (base 1) para COCO y `model.class_names` (base 0) para checkpoints propios.
- [x] `tests/test_rfdetr_detector.py` cubre car/bus/truck/motorcycle con ids COCO reales, nombres propios e ids desconocidos.

### TODO-036: Trabajo heredado sin terminar de conectar `added: 2026-09-14`
- [x] `carcounter/detection.py:detect_and_track` sin la rama heredada (COCO por defecto, `model.track`): exige tracker antes de inferir. `YOLODetector.track()` eliminado; `scripts/benchmark_pipeline.py` usa SORT explícito.
- [x] Validación de `inference_roi` unificada en `carcounter/geometry.py:validate_inference_roi()` para detección y cámara.
- [x] `predict_roi_boxes` y `_predict_roi_boxes` eliminados (sin llamadas).
- [x] `carcounter/engine.py` y `tests/test_engine.py` eliminados: copia anterior del loop de `main.py` sin caché, cámara, ROI ni eventos. Pausa y callbacks no los usaba ningún flujo: el wizard ejecuta `main.py` y la API da el estado en vivo.
- [x] Wizard con BoT-SORT; RF-DETR ya no fuerza SORT.
- [x] `run` del JSON registra `tracker` efectivo y `requested_tracker`; con fallback guarda parámetros de SORT.
- [x] `main.py` valida el perfil con `AppConfig.validate()` (ROI y deriva incluidas) y termina con código 1 antes de abrir el video.
- [x] `counting_events` en la DB (`events_json`, con migración de bases existentes) y en `/api/stats` (`events_count`, `recent_events`).

### TODO-019: Persistencia local libSQL
- [x] Pruebas con `libsql-experimental` instalado: guardado, lectura con conexión nueva, eventos y migración (`tests/test_db.py`).

### TODO-028: Alcance del aforo con EPS
- [x] Grupos acordados: `ligeros` (car, van, tricycle, awning-tricycle), `pesados` (bus, truck), `dos_ruedas` (motor, bicycle); mapeo en `carcounter/constants.py:CLASS_GROUPS`, bicicletas y triciclos ya pasan el filtro.
- [x] Eventos con `group`, `routes_by_group` en el JSON y `scripts/validate_routes.py --events --by-group` (`tests/test_class_groups.py`).
- [x] Alcance, tramo propuesto y propuesta de aceptación documentados en `docs/GUIDES/COUNTING_SCOPE.md`, con investigación en `docs/RESEARCH/COUNTING_ACCURACY_2026_09_14.md`.

### TODO-030: Reporte de tracks incompletos y posibles cambios de ID
- [x] El CSV de tracks exporta `origin_confirmed`, `class_votes`, `first_bbox` y `last_bbox`; las columnas viven en `carcounter/export.py:TRACK_CSV_FIELDS`.
- [x] `carcounter/track_review.py`: tracks con origen confirmado y sin destino (separa los visibles al final del tramo), resumen por acceso, fragmentación y candidatos a cambio de ID emparejados uno a uno, con distancia en tamaños del vehículo y clase de ambos tracks.
- [x] `scripts/review_incomplete_tracks.py` y `make review-tracks`: `report.json`, `report.md` y un recorte por track y momento con la caja real; decodifica los frames en orden y etiqueta con el frame del video.
- [x] `make run` y `make run-full` exportan también `tracks.csv`.

Notes TODO-030: en el replay del tramo de prueba (modo `lines`, sin zonas) no hay incompletos; con 80 px y 30 frames salían 678 candidatos, con 1.5 tamaños, 10 frames y emparejamiento uno a uno quedan 83. Suite: 364 pruebas.

### TODO-029: Tramos de cámara estable
- [x] `carcounter/video_segments.py:segment_by_stability()` divide un video en tramos donde la deriva respecto al primer frame del tramo no supera el umbral; los tramos cortos se marcan como transición y no tienen referencia.
- [x] `scripts/segment_video.py` y `make segment-video` generan `segments.json` y la imagen de referencia de cada tramo estable; umbral por defecto 10 px.
- [x] `main.py --start-frame` procesa un tramo; el seek es exacto (diferencia 0.0 contra decodificar en orden) y la caché incluye `start_frame` solo si es distinto de 1.

Notes TODO-029: en `glorieta_normal.mp4` el tramo 2:00 a 6:25 queda como un solo tramo estable (deriva máxima 9.8 px) y contiene el tramo oficial. Con `--start-frame 5396` sobre el video completo salen 5 eventos contra 6 del clip de prueba: el clip es otra codificación del mismo minuto y el detector ve cajas algo distintas. Suite: 357 pruebas.

Notes TODO-028: `make replay-aerial` conserva los 6 eventos (5 ligeros y 1 pesado). Suite: 352 pruebas sin omisiones en `env/` local.

Notes TODO-036: con `fastapi`, `httpx` y `libsql-experimental` instalados en `env/` la suite pasa 337 pruebas sin omisiones. `make replay-aerial` conserva los 6 eventos (IDs 24/60/19/67/47/146) y un perfil con ROI invertida termina con código 1.

Notes TODO-035: detectado en la auditoría del PR #4; antes se indexaba la lista `COCO_NAMES` de base 0, de modo que un auto salía como moto y buses/camiones se descartaban.

Notes: trabajo local del 6 al 14 de septiembre hecho por sesiones de Codex sin commits; recuperado en 61ffbf2 y c59f882 y mergeado en PR #4 (346ff5f). Mediciones en `docs/RESEARCH/*_2026_09_06/07/14.md`. Las validaciones humanas, la GUI en escritorio y la activación del cron siguen pendientes en TASK_TODO.md.
